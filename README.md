# otus_appium
Домашнее задание по теме Мобильное тестирование

Для запуска тестов нужно использовать в терминале команду
```
mvn clean test -DdatabaseUsername=<username> -DdatabasePassword=<password>
```
